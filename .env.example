# Path to the SQLite database file used by the self-hosted instance.
# Kept outside the repo/image so it survives redeploys when mounted as a volume.
DATABASE_URL=file:./data/contex01.sqlite

# Optional: set to "true" to seed a couple of demo contexts on first boot.
SEED_DEMO_DATA=false
